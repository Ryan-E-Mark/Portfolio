import react from "react";

const NavBar = () => {
    return (
        <section>
            <div>

            </div>
            <div>
                <a href="#about">About</a> 
                <a href="#work">Work</a> 
                <a href="#contact">Contact</a> 
            </div>
        </section>
    )
}

export default NavBar;
